# Python dependencies. Install inside the project environment:
#     python3 -m pip install -r requirements.txt
#
# External binaries (blastn, MFEprimer, minimap2, samtools, seqkit, kraken2)
# are NOT pip-installable; use `bash install.sh araclar`.

primer3-py>=2.0        # oligo thermodynamics (one of two independent Tm sources)
biopython>=1.79        # sequence handling, second independent Tm source
numpy>=1.21            # vectorised scanning in screening/engine_gateway.py
openpyxl>=3.0          # Excel deliverables
pysam>=0.19            # BAM/pileup for consensus and N-analysis
matplotlib>=3.5        # figures

# Optional. alignment.py tries mappy first and falls back to the minimap2 CLI,
# so this is not required -- but it is faster when present.
# mappy>=2.24
