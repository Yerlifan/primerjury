# Kod denetimi — 2026-08-21

Amaç: projeyi genel amaçlı bir GitHub aracına dönüştürmeden önce kodda ne
olduğunu ölçmek. Bu belgedeki her bulgu bu oturumda kodun kendisi okunarak
doğrulandı; projenin eski denetim raporlarından alınan iddialar ayrıca
kaynağında sınandı ve hangilerinin kapandığı işaretlendi.

## Kapsam

| | |
|---|---|
| Taranan Python dosyası | 279 (kanonik küme) |
| Toplam satır | 57.469 |
| Hariç tutulan | `MicRhoBooster/03_KODLAR` (kopya), `ALI/`, `_arsiv`, `_to_delete`, `_SILINECEKLER`, `__pycache__` |
| Çekirdek modüller | `KAPSAMLI_ARAMA/` (35), `KURTARMA/` (30), `WSL_betikleri/` (24), `TEK_PROTOKOL/`, `ORTAK_PUANLAYICI/`, `capraz_kontrol.py` |

## Temiz çıkanlar

Bunlar ölçüldü, tahmin değil:

- **Sözdizimi hatası: 0** (279/279 dosya `ast.parse` ile ayrıştırıldı)
- **Çıplak `except:`: 0**
- **Değişken varsayılan argüman (`def f(x=[])`): 0**
- **`subprocess(..., shell=True)`: 0**
- **`== None` karşılaştırması: çekirdekte 0**

57 bin satırlık bir kod tabanı için olağandışı derecede temiz. Kod hijyeni
sorun değil; aşağıdaki bulgular mantık ve dokümantasyon düzeyinde.

---

## A. Açık hatalar

### A1 — `DUZELTME_BEKLEYEN_2026-08-09/` klasörü bir tuzak · CİDDİ

Klasörün adı "bekleyen düzeltme" diyor ve `OKU_ONCE.md` içinde uygulama komutu
hazır duruyor:

```
copy "DUZELTME_BEKLEYEN_2026-08-09\quick_consistency_test.py.DUZELTILMIS" ^
     "KURTARMA\quick_consistency_test.py"
```

**Bu komut çalıştırılmamalı — kodu geriye götürür.** Ölçüm:

| | `.DUZELTILMIS` | kullanımdaki dosya |
|---|---|---|
| Tarih | 2026-08-09 16:48 | **2026-08-10 22:38** |
| Satır | 621 | **716** |
| `katman_denetimi` (H kapısı düzeltmesi) | var | **var** |
| `REFERANS_DOSYASI` / `referans_degerler.tsv` | **yok** | var |
| `CIKIS_REFERANS_BAYAT` | **yok** | var |

Kullanımdaki dosya daha yeni ve `.DUZELTILMIS` sürümünün üst kümesi: H kapısı
düzeltmesini zaten içeriyor, üstüne 10 Ağustos'ta eklenen bayat-referans
düzeltmesini de taşıyor. Kopyalama komutu bu ikincisini silerdi.

Kanıtı iki kez koşturdum (`sina_H_kapisi.py`, yedi senaryo):

- `.DUZELTILMIS` modülüne karşı: **7/7 doğru**, çıkış kodu 0
- Kullanımdaki `KURTARMA/quick_consistency_test.py`'ye karşı: **7/7 doğru**,
  çıkış kodu 0

Senaryo 1 (gerçek D çıktısı) eski mantıkta `HATA(1)` veriyor, her iki yeni
sürümde de geçiyor — yanlış alarm gitmiş. Senaryo 2, 3, 4, 6, 7 (gerçekten boş
ya da şeması kaymış katman) hâlâ yakalanıyor.

**Yapılacak:** klasörü uygulama değil **arşiv** olarak işaretle, `OKU_ONCE.md`
içindeki kopyalama komutunu sil. Repoya taşınmamalı.

**Yan bulgu:** `sina_H_kapisi.py` yeniden koşturulamıyor — iki ölü yol gömülü:
`/sessions/elegant-zen-babbage/mnt/PrimerTasarlama` ve
`/tmp/dn/h/hizli_duzeltilmis.py`. Kanıt betiği kendi kanıtladığı şeyi
tekrarlayamıyorsa kanıt değildir. Repoya girecek testler proje köküne göreli
yol kullanmalı.

### A2 — Katman 2 yalnız BOYA bakıyor, katman 3 ve 4 TAKSONA · CİDDİ

> **Düzeltme (aynı gün):** bu bulgu ilk turda "`global_scan.py:129` koşulsuz
> sayıyor, sütun adı yanlış" diye yazılmıştı. Yanlıştı. `global_scan.py` bir
> **ham tarayıcıdır**; `urun` alanının toplam vuruşu sayması doğru davranıştır.
> Ayrımı tüketen taraf yapar ve `dogrulama_turu.katman1_yerel` bunu 2026-08-06'da
> (D-1) zaten düzeltmiş: `ayni_boyda` / `hedef_disi` ayrı sayılıyor ve hükme
> yalnız `hedef_disi` giriyor. Gerçek boşluk aşağıdaki daha dar olan.

Dört doğrulama katmanı "bu ürün hedefin kendisinden mi geliyor, dışından mı"
sorusunu **farklı ölçütlerle** cevaplıyor:

| Katman | Ayrım ölçütü | Kaynak |
|---|---|---|
| 2 — yerel veritabanı | **yalnız BOY** (beklenen ± `BOY_TOL`) | `dogrulama_turu.katman1_yerel` |
| 3 — MFEprimer | **TAKSON** (klad içi / aynı alan klad dışı / farklı alan) | `mfe_katmani.klad_siniflandir`, D-12 |
| 4 — NCBI | **TAKSON** (organizma dışlama, `ENTREZ_QUERY`) | `ncbi_layer.py` |

Boy ölçütünün güvenilmez olduğu D-12'de zaten ölçülmüştü: MFEprimer'in "hedef
dışı" saydığı 1.605 amplikonun **%95,7'si (1.536) hedef kladın kendi
içindendi**, yalnızca boyu farklıydı. Gerçek çapraz yalnızca %0,9 (14 amplikon).
Katman 3 bu yüzden taksonomik süzgece geçti; **katman 2 geçmedi.**

Yani aynı yanılgı katman 2'de hâlâ duruyor ve özellikle grup/evrensel
primerlerde hedefin kendi uzunluk varyantları "hedef dışı" olarak hükme
giriyor.

**Veri hazır, kullanılmıyor.** Taksonomik ayrım için gereken her şey mevcut:

- SILVA/UNITE/PR2 başlıkları tam taksonomi taşıyor — ölçüldü:
  `>AY846379.1.1791 Eukaryota;Archaeplastida;Chloroplastida;Chlorophyta;...`
- `KAPSAMLI_ARAMA/hedef_klad.tsv` her hedefin alan + klad jetonlarını tutuyor
  (39 satır) ve `mfe_katmani.klad_siniflandir()` bunu okuyan çalışan bir
  sınıflandırıcı.
- `kuresel_tarama.tara()` her vuruş için başlığı zaten döndürüyor
  (`vurus: [(baslik, boy, mmF, mmR)]`).

**300 tavanı — ölçüldü.** `global_scan.py:131` `vurus` listesini 300 kayıtta
kesiyor (`if len(r['vurus']) < 300`). `urun` sayacının tavanı yok, kimliklerin
var. `DOGRULAMA_SONUC/dogrulama_uc_sutun.tsv`'deki gerçek sayılar:

| hedef | tüm vuruş | aynı boyda (hedefin kendisi) | "hedef dışı" |
|---|---:|---:|---:|
| Bakteri_universal | 483.098 | 288.832 | **194.266** |
| Mantar_universal (F1) | 159.382 | 152.166 | 7.216 |
| Microascaceae_askomikot | 74.444 | 74.433 | 11 |
| Bacteroidales_kumesi | 71.945 | 71.925 | 20 |
| Mantar_universal (F2) | 57.408 | 56.520 | 888 |

**22 hedefin 18'i tavanı aşıyor.**

Bu tablo iki şey söylüyor:

1. **Tavanı kaldırmak yanlış çözüm.** Bakteri_universal tek başına 483 bin
   vuruş; her biri 150 karakterlik başlık + üç tam sayı ≈ 100 MB. `tara()`
   bütün adayları aynı anda tutuyor, 12 veritabanı boyunca dönüyor ve her
   parçada pickle'a yazıyor. Tavanı kaldırmak birkaç GB bellek ve şişmiş
   kontrol noktaları demek — kesintiye dayanıklılığı da bozar.
2. **Doğru çözüm saklamak değil, saymak.** Taksonomik ayrım için kimliklerin
   listesi gerekmiyor; tarama sırasında her vuruşu D-12'nin dört sınıfına
   (a / ao / b / c) sokup sayaç artırmak yeterli. Bellek aday başına sabit
   kalır ve sayı **eksiksiz** olur. `vurus` listesi 300'de kalabilir — orada
   kanıt örneği olarak zaten yeterli.

Bakteri_universal'in 194.266 "hedef dışı"sının neredeyse tamamı bakteri 16S'i,
yani evrensel bir bakteri primeri için tasarım gereği hedef **içi**. D-12'nin
%95,7'lik bulgusunun aynısı, katman 2'de ve çok daha büyük ölçekte.

**Neden bu turda uygulanmadı:** bu düzeltme bilimsel hükmü değiştirir — bazı
çiftler RISKLI'den TEMIZ'e geçer. Projenin kendi kuralı gereği hüküm değiştiren
bir değişiklik ölçümle kanıtlanmadan ve karar verilmeden yapılmaz. Ayrıca D-1
yorumunun uyardığı şey tam da budur: katman 2 ile katman 3 **aynı soruyu**
sormaya başlayınca `birlestir()` davranışı değişir. Ayrı bir adım olarak,
öncesi/sonrası tablosuyla yapılmalı.

### A3 — `VURUS_ESIGI = 0` gerekçesiz · UYARI

`KURTARMA/specificity_round.py:54`

```python
VURUS_ESIGI = 0   # hedef disi urun sayisi bunun ustundeyse RISKLI
```

Tek bir vuruş RISKLI üretiyor. Bilinçli bir katılık olabilir — projenin
"siparişe tek yanlış primer gitmesin" kuralıyla tutarlı — ama gerekçesi kodda
yazmıyor. Projenin geri kalanı her sabitin gerekçesini yazdığı için bu bir
istisna. Genel amaçlı araçta bu değer kullanıcı tarafından ayarlanabilir olmalı
ve varsayılanın neden 0 olduğu belgelenmeli.

### A4 — Beklenen ürün boyu ayrıştırılamazsa katman sessizce düşüyor · UYARI

`KURTARMA/mfeprimer_layer.py:378-381`

```python
try:
    bek[re.sub(r'\W+', '_', c['hedef'])] = (c['hedef'], int(c['urun']))
except (TypeError, ValueError, KeyError):
    pass
```

`c['urun']` sayıya çevrilemezse o hedef `bek` sözlüğüne hiç girmiyor. Sonuç:
`_spec_ayristir()` o hedef için "aynı boyda" ayrımını yapamıyor ve her ürün
hedef dışı sayılıyor. Bozuk tek bir hücre, o satırın hükmünü sessizce
sertleştiriyor. En azından uyarı basmalı.

### A5 — Elle doldurulan NT şablonundaki yazım hatası sessizce yutuluyor · UYARI

`KURTARMA/specificity_round.py:861-865`

```python
try:
    out[r['hedef'].strip()] = dict(durum='TAMAM (elle)', hedef_disi=int(n), ...)
except ValueError:
    pass
```

İnsanın doldurduğu `NT_SONUC_SABLONU.tsv` dosyasında sayı alanına sayı olmayan
bir şey yazılırsa (örneğin `~3` ya da `3 (belki)`) o hedef için NCBI katmanı hiç
oluşmuyor ve hüküm tablosunda `BILINMIYOR` görünüyor. Kullanıcı değeri girdiğini
sanıyor, katman düşmüş oluyor. Bu, projenin kendi tanımladığı baş hata türünün
ta kendisi: *program hata vermeden yanlış cevap üretir.*

### A6 — Bozuk `.fai` satırı ortalama uzunluğu sessizce bozuyor · BİLGİ

`KURTARMA/identity_verification.py:419-421` — `.fai` indeksindeki bozuk bir satır
atlanıyor ama `t`/`n` birikimi sürüyor, yani ortalama uzunluk kısmi veriden
hesaplanıyor. Bu değer kısa liste puanlamasına giriyor. Etkisi küçük ama sessiz.

### A7 — Kullanım örnekleri karışık yönlü klasörü gösteriyor · CİDDİ (belge)

`WSL_betikleri/03_primer_aday_uret.py:24` ve `04_grup_primer.py:27-29`
docstring'lerindeki örnekler `"consensus sequences/..."` klasörünü işaret ediyor.

O klasör **karışık yönlüdür** (ölçülen: 71 antisense / 27 sense) ve
`config.py:39-45` onu doğrudan okumayı açıkça yasaklıyor:

> ters yönlü bir konsensüste in-silico PCR **SESSİZCE 0 ürün verir**
> (ölçülen kayıp %100)

Kodun kendisi doğru — yol CLI argümanı olarak alınıyor, gömülü değil. Ama
belgelenen örneği izleyen bir kullanıcı doğrudan bu tuzağa düşer. Örnekler
`konsensus_kanonik/` gösterecek şekilde düzeltilmeli.

---

## B. Denetleyicinin kendi hatası

`capraz_kontrol.py` M5-D9 kontrolü ("karışık klasör okuyan kod") **docstring ve
yorumları ayıklamıyor**, bu yüzden yalnızca açıklamada klasör adı geçen dosyaları
da RİSKLİ işaretliyor. 2026-08-09 koşusunda 5 yanlış pozitif üretti.

Aynı kontrolün doğru sürümü projede zaten var:
`KAPSAMLI_ARAMA/orientation_code_scan.py:48-52` içindeki `kod_govdesi()` fonksiyonu
tam olarak bu ayıklamayı yapıyor ve yorumunda "ilk taramada bu yüzden yanlış
pozitif çıktı" diye not düşülmüş.

İki tarayıcı aynı soruya farklı cevap veriyor. `capraz_kontrol.py` D9,
`yon_kod_taramasi.kod_govdesi()` kullanacak şekilde birleştirilmeli.

---

## C. Kapandığı doğrulanan eski bulgular

Bunları kodda sınadım, gerçekten düzeltilmişler:

| Bulgu | Durum |
|---|---|
| **D-2 totolojisi** — `n_ok` sabit `'TEMIZ'` olduğu halde uyuşma oylamasına giriyordu; 16 çiftin 16'sı mekanik olarak ÇELİŞKİLİ çıkıyordu | **Kapalı.** `specificity_round.py:1014-1023` — numune katmanı artık sütun olarak gösteriliyor ama oy vermiyor |
| **Yön normalizasyonu** — konsensüsler karışık yönlü klasörden okunuyordu | **Kapalı.** `config.py:44-47`, `KONSENSUS = KONSENSUS_KANONIK` |
| **D-10** — MFEprimer var olan çıktının üzerine yazmayıp 1 döndürüyor, ölçüm sessizce `dict(hata=...)` oluyordu | **Kapalı.** `mfeprimer_layer.py:316-320` eski çıktıyı siliyor |
| **MFEprimer izin hatası** — arşivden çıkan dosya izinsiz gelince katman sessizce "araç yok" diye atlanıyordu | **Kapalı.** `mfeprimer_layer.py:65-67` `chmod` deniyor |
| **H kapısı yanlış alarmı** — sütun adı kayması yüzünden dolu katman "boş" sayılıyor, zincir duruyordu | **Kapalı.** `katman_denetimi()` kullanımdaki dosyada var; yedi senaryoluk kanıt 7/7 geçti |
| **Bayat referans** — referans değerler koda gömülüydü, primer çifti değişince test yanlışlıkla "ZİNCİR TUTARSIZ" diyordu | **Kapalı.** `quick_consistency_test.py` referansları `HIZLI_TEST/referans_degerler.tsv`'den okuyor, her satırda ölçümün F/R dizisi yazılı; çift değişmişse karşılaştırma yapmıyor (`CIKIS_REFERANS_BAYAT`) |

---

## D. İşletim durumu

`KONTROL_SONUC/CAPRAZ_KONTROL_2026-08-09_2216.md` koşusundan:

- **3011 kontrol noktası dosyasının 2899'u bayat** (girdilerden eski). Bayat
  kontrol noktası, değişmiş girdiyle eski sonucu yeniden kullandırır. Koşu
  istatistiği bunu doğruluyor: *0 isabet, 112 ıska*.
- Çıkış kodu 6; 5 kontrol koşulamadı (bunlar "geçti" değil).
- 141 CİDDİ bulgunun 115'i M1 KİMLİK modülünden ve çoğu Kraken etiketi ile
  ölçülen kimliğin ayrışması — kod hatası değil, projenin asıl bilimsel bulgusu.

---

## E. Repoya taşımayı ilgilendiren yapısal notlar

**E1 — İkinci kod kopyası.** `MicRhoBooster/03_KODLAR/` altında bütün kod
ağacının ikinci bir kopyası duruyor (`KURTARMA/`, `WSL_betikleri/`,
`KAPSAMLI_ARAMA/`, `ALI/WSL/`). Satır sayıları biraz farklı, yani **ikisi de
düzenlenmiş** ve hangisinin güncel olduğu belli değil. Repoya tek kaynak
girmeli; hangisinin kanonik olduğu diff ile belirlenmeli.

**E2 — Yapılandırma zaten merkezi.** `KAPSAMLI_ARAMA/config.py` bütün
yolları ve sayısal sabitleri tek yerde tutuyor; dosyanın kendi başlığı
"Kullanıcı bu dosyayı düzenleyebilir" diyor. Genel amaçlı araca dönüşüm için bu
çok iyi haber: yolları YAML/TOML'a taşımak için tek dosya var, kod tabanına
dağılmış sabit yol avı gerekmiyor.

**E3 — Sabit yol sorunu çekirdekte değil.** `/mnt/c/Users/yerli/...` geçen 38
dosyanın büyük çoğunluğu `ALI/` altında. Çekirdek `--kok .` deseniyle çalışıyor.

**E4 — `ALI/` bağımlılığı gerçek.** `full_chain.py` kardeş klasördeki
`../ALI/WSL/130_KRAKEN_ARAC.sh` betiğini çağırıyor ve `ALI/0_TESLIM_HOCA/`
içine yazıyor. Repo tek köklü olacaksa bu betik içeri alınmalı.

---

## Öncelik sırası

| Sıra | İş | Neden önce |
|---|---|---|
| 1 | A1 — `DUZELTME_BEKLEYEN` klasörünü arşive çevir, kopyalama komutunu sil | Şu an aktif bir gerileme tuzağı |
| 2 | A7 — docstring örneklerini `konsensus_kanonik/` gösterecek şekilde düzelt | Kullanıcıyı sessiz hataya sokuyor, düzeltmesi birkaç satır |
| 3 | E1 — kanonik kod kaynağını belirle | Repoya ne gireceğini bu belirler |
| 4 | A2, A4, A5 — sessiz düşüşleri uyarıya çevir | Hüküm doğruluğu |
| 5 | B — iki tarayıcıyı birleştir | Denetim sonuçlarına güven |
| 6 | A3 — eşiği belgele ve ayarlanabilir yap | Genelleştirme işinin parçası |

## Uygulanan düzeltmeler — 2026-08-21

| Bulgu | Dosya | Ne yapıldı | Kanıt |
|---|---|---|---|
| A1 | `DUZELTME_BEKLEYEN_2026-08-09/OKU_ONCE.md` | Klasör arşiv olarak işaretlendi, gerileme yaratan kopyalama komutu devre dışı bırakıldı, ölçüm tablosu eklendi | `sina_H_kapisi.py` iki sürüme karşı 7/7 |
| A7 | `WSL_betikleri/03_primer_aday_uret.py`, `04_grup_primer.py` | Kullanım örnekleri `konsensus_kanonik/` gösteriyor; karışık yönlü klasörün neden yasak olduğu ve `INDEKS.tsv`'den okunması gerektiği yazıldı | dosya adları `konsensus_kanonik/INDEKS.tsv` ile doğrulandı |
| A4 | `KURTARMA/mfeprimer_layer.py` | Beklenen ürün boyu okunamayan hedefler artık sessizce düşmüyor, adıyla ve hata türüyle uyarı basılıyor | sözdizimi + `yaz` kapsamda |
| A5 | `KURTARMA/specificity_round.py` | `ncbi_yukle()` bozuk sayı alanlarını topluyor ve uyarıyor; BOŞ ("bakmadım") ile BOZUK ayrımı belgelendi | çağrı yerine `yaz` geçildi |
| A6 | `KURTARMA/identity_verification.py` | Bozuk `.fai` satırı varsa indeks **tamamen** yok sayılıyor ve FASTA taranıyor; kısmi ortalama üretilmiyor, stderr'e uyarı | sözdizimi + `sys` içe aktarılmış |
| A3 | `KURTARMA/specificity_round.py` | `VURUS_ESIGI` gerekçesi yazıldı, `PT_VURUS_ESIGI` ile ayarlanabilir yapıldı, varsayılan 0 korundu; hangi eşikle koşulduğu hem TSV başlığına hem rapora yazılıyor | 4 yerde geçiyor, varsayılan değişmedi |
| B | `capraz_kontrol.py` | D9 kontrolü üç süzgeçle keskinleştirildi: docstring/yorum ayıklama, çıktı çağrısı argümanları, meşru okuyucular muafiyeti. Bulgu mesajı artık satır numarası veriyor | `DENETIM_SINAMALARI/sina_D9_karisik_klasor.py` 7/7 |

**Yeni:** `DENETIM_SINAMALARI/` — kalıcı sınama klasörü. İçindeki sınama proje
köküne göreli çalışır, gömülü yol taşımaz (`sina_H_kapisi.py`'nin aksine).

**B düzeltmesinde yol boyunca bulunan iki ek hata:**

1. Özgün `yon_kod_taramasi.kod_govdesi()` dizeleri **satır sayısına** göre
   eliyor (`end_lineno - lineno >= 1`). Bu iki yönden de yanlış: tek satırlık
   docstring atılmıyor (yanlış pozitif), kodda gerçekten kullanılan çok satırlı
   dize atılıyor (yanlış negatif). Doğru ayırt edici, dizenin bir `ast.Expr`
   altında olup olmadığıdır. `capraz_kontrol._kod_govdesi()` düzeltilmiş
   ölçütü kullanıyor.
2. Gövde üretirken docstring satırlarını **atmak** satır numaralarını kaydırıyor
   ve AST'ten gelen satır kümeleriyle hizalamayı bozuyordu — ölçüldü
   (`KAPSAMLI_ARAMA/run_all.py` özgün satır 222, kırpılmış gövdede 183). Artık
   satırlar atılmıyor, boşaltılıyor.

**Kalan:** A2'nin hükme bağlanması — aşağıdaki ölçüm sonrası **yapılmaması
gerektiği** anlaşıldı.

## A2 ölçümü tamamlandı — ve sonuç değişikliği DURDURDU

Beş taksonomi taşıyan veritabanı tarandı (SILVA SSU 1187 sn, SILVA LSU 369 sn,
UNITE 1686 sn, PR2 438 sn, ROD 460 sn). 22 çift, tam kapsam.

### Bulgu 1 — evrensel bakteri primerinde taksonomi kesin haklı

| ölçüt | Bakteri_universal |
|---|---:|
| BOY tabanlı "hedef dışı" | **171.080** |
| TAKSON tabanlı (b+c) | **122** |
| klad içi (a) | 419.750 |
| organel (ao) | 4.726 |

430.534 vuruşun 419.750'si bakteri — yani evrensel bir bakteri primeri için
**tasarım gereği hedef içi**. Boy ölçütü bunların 171.080'ini "hedef dışı"
sayıyordu. D-12'nin %95,7'lik bulgusu, tam ölçekte doğrulandı.

`ao` = 4.726 kloroplast/mitokondri ayrıca sınıflandırıcının çalıştığının
bağımsız kanıtı: evrensel 16S primerlerinin organel çoğaltması bilinen bir
sorundur ve ölçüm onu kendiliğinden buldu.

### Bulgu 2 — diğer 21 hedefte taksonomi TEK BAŞINA kullanılamaz

| hedef | BOY | TAKSON |
|---|---:|---:|
| Microascaceae_askomikot | 11 | **69.786** |
| Bacteroidales_kumesi | 20 | **43.546** |
| Mantar_universal (F1) | 6.626 | 43.286 |
| Proteolitik_Synergistaceae | 15 | 16.782 |
| Petrimonas_cinsi | 0 | 651 |

Microascaceae için 69.786 gerçek çapraz reaksiyon olamaz — sipariş edilmiş bir
çift için bu sayı anlamsız.

**Sebep katman 2'nin tarama ölçütünde ve kodda zaten yazılı:**

> Bu katmanin olcutu 3' son iki baz sarti TASIMAZ, yani gercek PCR olcutunden
> DAHA GEVSEKtir — `specificity_round.py:247`

≤5 uyumsuzluk, 3' kenetlenme şartı yok. Korunmuş bir işaretçi geninde bu ölçüt
neredeyse her kayda bağlanır. **Boy penceresi (±10 bp) kazara bir sıkılık
süzgeci olarak çalışıyordu** — yanlış sebeple, ama gürültüyü bastırıyordu.

Taksonomi *hangi klada ait* sorusunu doğru cevaplıyor; ama ham `b` sayısı
taramanın gevşekliğini miras alıyor. Boy yerine taksonomiyi tek başına koymak
hükmü RISKLI'ye boğardı.

### Karar: hüküm DEĞİŞTİRİLMEDİ

Taksonomik sütunlar rapora **tanı olarak** girdi, hükme girmedi. Doğru bileşim
taksonomi **artı sıkılık** — ki bunu MFEprimer katmanı zaten yapıyor:
`TM_MARJI` ile "oluşabilir"/"oluşmaz" ayrımı (`mfeprimer_layer.py:474`).

**Sonraki adım (ayrı iş):** katman 2'ye 3' kenetlenme şartı ya da Ta yakınlığı
eklendikten sonra taksonomik sayı hükme bağlanabilir. Öncesinde bağlanmamalı.

Bu, ölçmeden değiştirmeme kuralının işe yaradığı yer: taksonomik ayrım
doğruydu, ama tek başına hükme bağlansaydı panelin 21 çiftinin 21'i haksız
yere RISKLI olurdu.

## Yeni: tek kurulum kapısı — `KUR.sh`

Dağınık kurulum betikleri tek kapıda toplandı (`ALI/WSL/01_kurulum.sh`,
`87_MFEPRIMER_KUR.sh`, `download_refseq.py`, `QIIME2_PICRUST_KOSU.bat`).

```
bash KUR.sh durum                 # ölçer, hiçbir şey değiştirmez
bash KUR.sh araclar               # kraken2, bracken, minimap2, samtools, blast, seqkit
bash KUR.sh veritabani            # SILVA + PR2 + RefSeq (+ UNITE/ROD yönergesi)
bash KUR.sh kraken-indir          # hazır Kraken2 veritabanı (k=35 sabit)
bash KUR.sh kraken-kur --kmer 31  # KENDİ k-mer'inle kendi veritabanını kur
bash KUR.sh qiime                 # QIIME2 + PICRUSt2 + SILVA sınıflandırıcı
bash KUR.sh hepsi
```

Üç ilke, projenin kendi kurallarından türetildi:

1. **İndirilen dosya doğrulanır, adrese güvenilmez.** Her indirmeden sonra
   dosya ölçülür: gerçekten FASTA mı, kaç kayıt, RNA mı DNA mı. Ölçüm tutmazsa
   dosya `.SUPHELI` olarak işaretlenir ve **kullanıma alınmaz**. Asgari kayıt
   sayıları gerçek ölçümlerden alındı (SILVA SSU 510.495, UNITE 2.069.189 vb.).
2. **Her adım yeniden çalıştırılabilir.** Doğrulanmış dosya atlanır, yarım
   indirme `wget -c` ile devam eder.
3. **Hiçbir şey sessizce atlanmaz.** Kurulamayan her şey sonda listelenir ve
   çıkış kodu sıfır olmaz.

**Kraken2 k-mer seçimi** (`kraken-kur --kmer/--minimizer/--bosluk`): hazır AWS
veritabanları k=35, l=31 ile kurulmuştur ve k seçilemez. Kendi k'nizi istiyorsanız
veritabanı sıfırdan kurulmalı. Betik kurulum parametrelerini
`$DB/KURULUM_BILGISI.txt` dosyasına yazar — `opts.k2d` ikili olduğu için hangi
k ile kurulduğu aksi halde altı ay sonra bilinemez.

Betiğin k-mer bölümü şunu açıkça söylüyor: **k-mer seçimi tek başına kimlik
sorununu çözmez.** Kraken ayrışmasının sebebi k değil, LCA'nın kendisi ve
veritabanı kapsamı. Hangi k ile kurulursa kurulsun kimlik iddiaları
`identity_verification.py` ile bağımsız sınanmalı.

**Yol boyunca bulunan hata:** `durum` ilk sürümünde `kraken2`, `bracken` ve
`seqkit` "YOK" raporluyordu. Üçü de **kuruluydu** — `micromamba`'nın `mikro`
ortamındaydı ve o ortam etkin değilken `PATH`'te görünmüyorlardı. Yalnız `PATH`'e
bakan bir denetim, doğru kurulmuş bir sistemi bozuk gösterir. Artık ortam da
aranıyor ve nerede bulunduğu yazılıyor. Ayrıca sürüm okuma bayrağı araca göre
seçiliyor (`blastn -version`, `seqkit version`, `bracken -v`); yanlış bayrak
"VAR USAGE" gibi anlamsız satırlar üretiyordu.

## F. Kimlik doğrulamada bulunan iki hata

### F1 — Adsız çevre kayıtları TÜR adı sayılıyordu · CİDDİ

`KURTARMA/identity_verification.py` · `ad_coz()`

NCBI nt gibi kümeler adlandırılmamış kayıtlarla dolu. `ad_coz`'un ikinci düzenli
ifadesi (`\b[A-Z][a-z]{3,}\s+[a-z]{3,}\b`) bunları ikili ad sanıyordu. Canlı
çalıştırıp ölçtüm:

```
'KJ734864.1 Uncultured prokaryote clone D5...'  →  cins='Uncultured', tür='Uncultured prokaryote'
'GQ503828.1 Bacterium enrichment culture...'    →  cins='Bacterium',  tür='Bacterium enrichment'
```

Kimlik %99 olduğu için `savunulabilir_duzey()` bunu TÜR düzeyinde bir ad sayıyor
ve iddia **DOĞRULANDI** damgası alıyordu. Yani cevapsızlık, onaylanmış kimlik
gibi raporlanıyordu. 12 iddianın **4'ü** bu durumdaydı.

Tuzak projede zaten biliniyordu ve iki yerde süzülüyordu
(`exclusion_coverage_check.py:103`, `order_classes.py:200`);
`ncbi_layer.py` docstring'i de belgeliyor. Adlandırmanın **tek sorumlusu** olan
modülde yoktu — "uncultured" için sıfır eşleşme.

**Düzeltildi.** Adsız kayıt artık ad üretmiyor; ayrı bir hüküm dalı eklendi
(`ADLANDIRILAMIYOR (referans adsız)`) çünkü mevcut "ad verilemez" dalı gerekçe
olarak "kimlik cins eşiğinin altında" yazıyordu — oysa kimlik %99. Yanlış
gerekçe, okuyanı "daha iyi referans bulunursa ad çıkar" sanmaya iter; sorun
referansın yakınlığında değil, **adsızlığında**.

Süzgecin fazla geniş olmaması kritik: gerçek adları susturmak daha sinsi bir
hata olurdu. `DENETIM_SINAMALARI/sina_adsiz_kayit.py` ikisini birden sınıyor
(12/12): adsız kayıtlar elenirken *Petrimonas sulfuriphila*, *Methanosarcina
mazei*, *Bacteroides fragilis* bozulmadan çözülüyor.

**Ölçülen etki:** 12 iddianın 4'ünde önerilen ad düştü.

| no | hüküm | önce | sonra |
|---|---|---|---|
| 4 | DOĞRULANDI | TÜR / *Uncultured prokaryote* | ADLANDIRILAMIYOR |
| 5 | DÜZELTİLMELİ | TÜR / *Uncultured bacterium* | ADLANDIRILAMIYOR |
| 6 | DOĞRULANDI | TÜR / *Bacterium enrichment* | ADLANDIRILAMIYOR |
| 12 | DÜZELTİLMELİ | TÜR / *Uncultured bacterium* | ADLANDIRILAMIYOR |

Diğer 8 iddia etkilenmedi — *Petriella setifera*, *Petrimonas* sp. ve
*Methanosarcina mazei* gerçek adlandırılmış referanslara dayanıyor.

### F2 — Kontrol noktası TÜRETİLMİŞ HÜKMÜ önbelleğe alıyordu · CİDDİ

Bu, F1'i düzelttikten sonra ortaya çıktı ve daha derin bir kusur.

F1 düzeltmesi uygulandıktan sonra tur yeniden koşuldu: 20 saniyede bitti, çıkış
kodu 0, "12/12 önceki koşudan alındı". Öncesi/sonrası karşılaştırıldı:
**12 satırın 12'si de aynı.** Düzeltme hiç çalışmamıştı.

Sebep: `KIMLIK_SONUC/kontrol/iddia_*.json` iddianın **tümünü** saklıyordu —
`adlandirma` ve `hukum` dahil. `AYAR_IMZASI` ise yalnızca **tarama**
parametrelerini mühürlüyor. Sonuç: hüküm mantığındaki bir düzeltme yeniden
koşuda sessizce yok sayılıyor ve eski hüküm geri geliyor.

Bu, denetleyicinin kendi işaretlediği bayat-kontrol-noktası sınıfının
(M5-D4, 3011'in 2899'u) en zararlı biçimi: *düzeltmenin uygulandığını
sanıyorsun.*

**Düzeltildi — mimari ayrım:**

- Adlandırma bloğu `adlandirmayi_turet()` fonksiyonuna çıkarıldı; taze koşu ve
  yeniden türetme **aynı kodu** kullanıyor. (Bu kod tabanında "aynı kontrolün
  iki sürümü çelişiyor" hatası zaten iki kez ölçüldü — B bulgusu ve
  `_kayit_coz`/`taksonomi` ayrışması. Üçüncüsü açılmadı.)
- Kontrol noktası artık **ham isabetleri** de saklıyor (`_ham_isabet_var`).
- Ham ölçüm varsa **tarama atlanıyor ama hüküm yeniden türetiliyor**.
- Eski biçimli kontrol noktası sessizce kabul edilmiyor: *"ham isabet yok,
  hüküm yeniden türetilemez, YENİDEN TARANIYOR"*.

İlke: **pahalı olan taramadır (saatler), hüküm saniyeler süren bir türetmedir.
Türetme her koşuda yeniden yapılmalıdır.**

Ölçülen sonuç: yeniden koşu artık *"tarama önbellekten, hüküm YENİDEN
türetildi"* diyor ve saniyeler sürüyor. 223 `vtb_*` tarama önbelleği ve 8 `nt_*`
NCBI kaydı korundu.

### F3 — En yakın 5 organizma eklendi

İstek üzerine ilk 3'ten 5'e çıkarıldı. Üç tasarım kararı:

1. **Organizma bazında tekilleştirme.** Havuzda 13 veritabanından isabet var ve
   aynı tür çoğu kümede bulunuyor; tekilleştirilmeseydi "en yakın 5" kolayca
   aynı organizmanın 5 kaydı olurdu.
2. **Adsız kayıt `-` olarak basılmıyor.** Neyle eşleşildiği görünmezse okuyan
   kişi %99'luk bir satırı sessizce ciddiye alır. Artık kaydın kendi tanımı
   `adsiz:` önekiyle yazılıyor.
3. **"Adsız" ile "türe inilmemiş" ayrıldı.** SILVA kayıtlarında tam taksonomi
   var ama tür ikilisi olmayabilir; bunlara "adsız" demek, sınıflandırılmış bir
   kaydı çevre klonuyla aynı kefeye koyardı. Artık en derin taksonomik jeton
   yazılıyor: `uncultured bacterium (Petrimonas)`, `Synergist (Synergistales)`.

Örnek — iddia 4, düzeltmeden sonra:

```
1) adsiz: Uncultured prokaryote clone A13 16S...  %99,00  [NCBI nt]
2) Methanosarcina vacuolata                       %97,57  [RefSeq arke 16S]
3) Methanosarcina barkeri                         %97,40  [RefSeq arke 16S]
4) Methanosarcina mangrovi                        %97,35  [RefSeq arke 16S]
5) Methanosarcina siciliae                        %97,27  [RefSeq arke 16S]
```

Bu tablo iddianın kendisini de doğruluyor: iddia "M. vacuolata'ya %97,6" diyordu,
ölçüm %97,57 veriyor.

## Yöntem notu

A1 ilk turda "düzeltme uygulanmamış" diye kaydedilmişti; dayanak yalnızca
`diff`in fark döndürmesiydi. Bu yetersiz bir çıkarımdı — iki dosya, biri
diğerinin üst kümesi olduğunda da farklı görünür. Tarih, satır sayısı ve
özellik varlığı ölçülünce yön tersine döndü. Repoya girecek her denetim
kuralının aynı sınavdan geçmesi gerekiyor: *fark var* ile *eksik var* aynı şey
değil.
